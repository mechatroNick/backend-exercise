# BKM — Track 05 test integrity mechanics

## Configuration

PREFIX=BKM
INPUT_FILES=.tracks/05-mandatory-quality-gate/SPEC.md, .tracks/05-mandatory-quality-gate/PLAN.md, .tracks/ADR/ADR-006-engineering-verification-and-closure-evidence.md
INTERESTED_TOPIC=Track 05 test integrity mechanics
STYLE_GUIDE=style-technical-schematic
CONTENT_GUIDE=content-guide-technical
CONTENT_DENSITY=high
CONTEXT_LENGTH=900
RESOLUTION=2K
RATIO=16:9
OUTPUT_DIR=.teach/prompts
OTHER_REQUIREMENTS=Fact-check source claims; no skip loophole, hidden retry, or unrun pass claim.

## Source anchors and bounded claims

Mandatory evidence starts from collection and explicit scope, then preserves assertions, command/result provenance, cleanup, and rejection of bypasses. Deterministic proof and a real-process Bash harness complement each other; neither replaces the other.

## Technical style instruction

Use a clean flat technical schematic with named source components, visible arrow direction, a pure white background, and muted category colors. Metadata tables and ASCII are authoring context only, never rendered illustration content.

## Abstracted teaching intent

Teach the governing invariant and evidence boundary of Track 05 test integrity mechanics without inventing topology, public behavior, or external actions.

## ASCII layout plan

```
manifest -> collector -> deterministic test lanes -> assertion recorder -> integrity checks -> actual receipt\nprocess harness --------------------------------------------------------^\nskip/xfail/deselect/mask/stale -> reject -> repair and rerun
```

## Component inventory

1. Requirement/evidence manifest
2. test collector
3. unit/integration lane
4. OpenAPI runtime lane
5. query-count lane
6. migration lane
7. JSON Lines lane
8. assertion recorder
9. integrity comb
10. Bash process harness
11. TEST-REPORT

## Arrow connection table

| From | To | Visible label |
| --- | --- | --- |
| manifest | collector | required scope |
| collector | lanes | collected selectors |
| lanes | recorder | executable assertions |
| process harness | recorder | independent actual process |
| recorder | integrity comb | result/provenance |
| comb | report | green only |
| comb | repair | reject bypass |

## Distilled Image Prompt

Flat 2D vector technical schematic on a clean white background displaying Track 05 test integrity mechanics. Monochromatic professional color scheme with subtle muted color accents, engineering-grade precision. No 3D effects, no gradients, no photorealism. Use dark-gray arrows, royal-blue records, muted-orange execution, forest-green verified artifacts, deep-purple internal flow, brick-red rejection, and rose-pink safe observability. Wide 16:9 landscape composition, high information density, pure white canvas, purposeful whitespace, short clean sans-serif labels, and integrated annotations only. The focal point is an orange-and-blue verification assembly labeled Mandatory Test Integrity.

The source manifest feeds a collector marked >=10 meaningful and then five orange lanes: Unit + Integration, Runtime OpenAPI, Query Count / N+1, Migrated DB, and JSON Lines. Runtime OpenAPI receives generated schema from /docs and checks actual success plus documented errors. Query Count contains the isolated stats reader and named parameters. The database lane shows upgrade, downgrade, re-upgrade without automatic schema creation. The logging lane uses safe labels source, UTC, redaction, and one exception owner; private tokens and content are crossed out. All lanes visibly socket into an Assertion + Result Recorder holding command, selector, exit result, and cleanup. A separate deep-purple Bash Process Harness enters after deterministic lanes with actual server, isolated port, and upstream harnesses, visually supplementing rather than replacing deterministic proof. A brick-red Integrity Checks comb has exactly six rejection tabs: skipped, xfail, deselected, masked, assertion-free, and stale/unrun. Its failed path loops to the responsible lane; only a green path emits TEST-REPORT. Include a small slate planned command is not pass card and a semantic-change route to ADR / SPEC review.

Fan the collector into five exact lanes: Unit + Integration, Runtime OpenAPI, Query Count / N+1, Migrated DB, and JSON Lines, then converge only at Assertion + Result Recorder. Schema connects to actual success/error cards; migration uses upgrade/downgrade/re-upgrade; query uses isolated stats-reader/named-parameters. Bash Process Harness joins independently. Six red integrity tabs each return to its affected lane; no plan card reaches the green report. Use thick dark arrows for contractual or durable authority, thin deep-purple arrows only for bounded wake-up or harness links, forest-green arrows only after actual complete evidence, and brick-red arrows for the named reject/retain paths. Keep every label short and adjacent to its connector. Render public contract surfaces as generic safe schemas, while tokens, identities, content, raw exception text, and private correspondence are absent. Use whitespace to separate private state from public routes and evidence from owner-only actions; do not add generic infrastructure to fill empty regions.

Give Runtime OpenAPI a matched pair of compact cards: green documented success and red documented error, both checked against one blue generated-schema sheet. Give JSON Lines a physical redaction sieve before the recorder, and label the process harness arrow invokes upstream gates. Put assertion-free beside a hollow checkmark, and stale/unrun beside an empty receipt sheet, so each integrity failure has a different visual cue rather than repeated warning symbols.

EXCLUDE: photorealism, 3D effects, gradients, shadows that look physical, graph paper, decorative illustrations, text tables, separate legends, generic invented topology, unreadable paragraphs, credentials, tokens, user content, raw logs, public weekly history, external push, submission, deployment, or any claim that an external action occurred.

## Preflight checklist

- [x] Source anchors are limited to the configured Track and ADR records
- [x] Main visual component count is within the 8–14 medium-density target
- [x] The final payload starts with the mandated style opener and ends with EXCLUDE
