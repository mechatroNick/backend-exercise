TRACK 07 — PRIVATE WEEKLY PROJECTIONS AND IMMUTABLE CORRECTIONS

Purpose: teach the delivered private historical extension without confusing it with the
public current `/stats` contract. Read with `.tracks/07-weekly-projections/SPEC.md`,
ADR-005, ADR-009, `app/bookmarks/stats/projection_models.py`,
`app/bookmarks/stats/projection_repository.py`,
`app/bookmarks/stats/projection_service.py`, and `app/bookmarks/stats/weekly.py`.

ARCHITECTURE VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector private event-time schematic, 2K wide 16:9, pure-white background. Calendar tiles must read as event-time windows: use Monday labels only,
arrows flowing across them, and a distinctive orange outline only around the current
open week. The working row is a single mutable-looking blue card with a small refresh
icon; developed points are stacked blue cards with locked icons and compact revision
badges. The supersedes link is an explicit curved arrow from newest point to prior point,
not a replacement arrow. The singleton state is a small blue control box that baseline
runner touches before and after each page of candidates. Make the calculator an orange
gear consuming canonical data through thin blue arrows, then output a payload envelope
and fingerprint disc. The privacy wall is tall and red with a simple route-stop symbol;
the public stats route beyond it must look separate and smaller. Use magenta dashed
workflow arrows sparingly, solid durable arrows for all private writes, and no accidental
arrow from private points to public response.

VISUAL DETAIL: Generate a 2K wide 16:9 private event-time timeline schematic. Put a
wide Monday-to-Monday UTC calendar ribbon across the top, with an orange open-week tile
near center and blue closed-week tiles on both sides. Under the orange tile, use a blue
replaceable “working” database-row card; under closed tiles, stack blue “point” cards
with small revision badges and a clear supersedes arrow. The lower middle contains three
large private blue storage shapes: working rows, append-only points, singleton state.
Use a magenta baseline-runner capsule entering state from left, then advancing through
small owner/window page cards to a green active state; use a second magenta projection
consumer entering from a dirty-marker capsule. Make the orange canonical calculator a
gear card between timeline and storage, with tiny payload/hash tags. On the far right,
draw a tall brick-red privacy wall between private tables and a small purple public stats
route; behind the public side, only current snapshot/live-SQL symbols may appear. Use
solid arrows for durable writes, dashed magenta arrows for workflow, red blocked arrows
for forbidden public-history exposure. No legend or decorative calendar art.

Flat 2D vector event-time data schematic on pure white. Wide 16:9 landscape, a
left-to-right UTC weekly timeline above three private blue tables. Magenta workflow,
teal analytics, blue persistence, orange calculation, rose readiness, red privacy
boundary.

At top, draw Monday-to-Monday UTC weeks as connected calendar blocks. Highlight one
orange “open current week” block and several blue “closed past week” blocks. Under the
open week, place a blue private `working` row with annotation “replaceable current
aggregate”. Under closed weeks, place stacked blue `point` cards labeled “revision 1”,
“revision 2”, with arrows from newer to prior labeled “supersedes”. Make an orange
calculator read canonical bookmarks using half-open event-time boundaries and produce
compact stable payload plus hash.

Below, show three private persistence components: `working rows`, append-only `points`,
and singleton `projection state`. A magenta “baseline runner” enters state, pages
through surviving users/windows, writes only evidenced rows, then reaches “active”. It
does not invent empty historical weeks. A material bookmark dirty marker feeds a
magenta projection consumer. For an open week it replaces working row; for a closed week
it either appends correction when hash changed or does nothing when hash matches.

At far right, place a large red privacy wall “no public weekly-history route”. Behind it,
show public `/stats` still taking only fresh current snapshot or live canonical SQL.
Below, readiness can report projection issue but liveness/current stats remain correct.
EXCLUDE exposed user history API, mutable rewritten past points, broker/second worker,
invented empty weeks, sensitive event content, 3D, gradients, and tables.

CRITICAL IMPLEMENTATION VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector consistency schematic, 2K wide 16:9, pure-white background. In deterministic identity panel, render payload as a sealed teal document
with a small “canonical” tag, version as a narrow blue wrapper, hash as a fingerprint
circle. The red mismatch barrier should block before any storage outcome. The outcome
diamond must show four distinctly shaped cards: working refresh, first locked point,
gray no-op, successor point with curved predecessor link. In generation panel, use one
large blue marker card with two side-by-side completion slots; arrows from current and
projection consumers must be symmetric until they meet at both-complete join. Place the
new G+1 marker slightly above/right so it visibly survives stale G acknowledgement.
Baseline status cards stack vertically and carry a small restart arrow. Readiness result
is rose and public response absence is explicit. Keep state labels short, all technical
details integrated, no legend, code, 3D, gradients, or tables.

VISUAL DETAIL: Generate a 2K wide 16:9 paired consistency schematic. Make left half a
teal/orange deterministic-identity pipeline: owner-scoped weekly SQL → canonical UTF-8
payload envelope → version wrapper → SHA-256 fingerprint. Place a tall red fail-closed
barrier at version mismatch, then a four-way outcome diamond leading to replace working,
first point, same-hash no-op, or append successor. Each outcome is a concrete blue row
card with short revision/hash annotation. Make right half a blue dual-generation marker
record G with two distinct completion badges for current and projection consumers; both
travel through guarded-ack gates and remove marker only at a green both-complete join.
Show G+1 arriving as newer blue marker that bypasses stale acknowledgement. Include a
small state stack pending/running/active and a rose readiness result below. Solid arrows
show durable order, dashed arrows retry, red arrows disabled/fail conditions. No public
API shapes except crossed-out history route; no 3D, gradients, tables, or long prose.

Flat 2D vector consistency-and-correction diagram on pure white. Split into two panels:
“deterministic identity” and “dual-generation completion”.

Left panel: owner-scoped, half-open weekly SQL produces a compact UTF-8 canonical payload
wrapped with schema/version identity. A domain-separated SHA-256 hash is compared only
inside the same calculation version; draw a red barrier “version mismatch: fail closed,
no auto rewrite”. Four outcomes branch: open week → replace working row; first closed
calculation → append revision 1; same hash → no-op; changed hash → append successor
revision linked to predecessor. Add a subtle privacy-deletion cascade arrow removing a
user’s private rows when account is deleted.

Right panel: one durable marker at generation G contains two completion badges “current
consumer” and “projection consumer”. Current refresher and projection processor each
read and guarded-complete only the generation they observed. The marker can disappear
only when generation and both completion values equal G; show a newer generation G+1
surviving a stale acknowledgement. Include a restartable baseline checkpoint with
statuses pending → running → active, and a green `projection enabled` vs red disabled
readiness branch. Edge chips: Monday boundary, late change, same-hash no-op, race,
restart, deleted user. Exclude public response changes, automatic recompute on version
mismatch, code listings, 3D, gradients, and tables.
