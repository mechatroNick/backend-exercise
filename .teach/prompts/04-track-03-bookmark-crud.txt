TRACK 03 — BOOKMARK CRUD, NORMALIZED TAGS, OWNERSHIP, AND TIMESTAMPS

Purpose: teach the protected resource behavior that makes the core API useful. Read
with `.tracks/03-bookmark-crud/SPEC.md`, `app/bookmarks/`, and Track 03 tests.

ARCHITECTURE VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector protected-resource schematic, 2K wide 16:9, pure-white background. Place User A above User B on the far left with equal charcoal silhouette
cards to show neither has privileged visibility. Their token badges connect to one router
without displaying token text. The central service is the biggest orange card and holds
three compact internal slots for validation, mutation policy, and tag normalization. The
data cluster is arranged with bookmarks central, tags beside it, and the link table below
as a slim bridge. Make the cross-user forbidden attempt a dashed red arrow that never
touches another user’s data halo; it lands directly at the purple not-found envelope.
Render raw tags as two slightly messy gray chips becoming one clean blue canonical tag
chip after the funnel. Make created_at a locked calendar and updated_at a rotating clock;
place both next to the bookmark card. The event should be a tiny magenta envelope after
the green commit stamp, visually secondary to the durable mutation. Use labels short
enough to render and no user-content examples beyond the two tag variants.

VISUAL DETAIL: Generate a 2K wide 16:9 protected-resource schematic. Make two
charcoal user silhouettes on the far left, one purple router behind them, a large orange
Bookmark Service in the center, and a royal-blue SQLite relationship cluster on the
right. Render authenticated identity as a brick-red badge physically attached to every
service/repository arrow. Use a large blue boundary halo around User A rows and show
User B’s cross-owner request as a thin red arrow ending at a compact purple 404 envelope.
Inside the database cluster, use rectangular table cards for bookmarks/tags, a thinner
link card for bookmark_tags, and clear many-to-many connectors. Beneath the central
service, make tag normalization a four-stage orange funnel; place timestamp lock and
refresh icons on two small blue bookmark metadata cards. Use solid arrows for committed
mutation, dashed gray arrows for semantic no-op, and one magenta arrow after a green
commit stamp for event publication. Every visible component gets a short label and
integrated annotation; no legend, tables, or generic user-interface screens.

Flat 2D vector technical schematic on a pure white background showing owner-scoped
bookmark CRUD. Wide 16:9 landscape, left-to-right request flow, medium-high learning
density. Charcoal client, purple API, red authenticated identity, orange service/policy,
blue persistence, magenta post-commit event.

At far left, two distinct user silhouettes “User A” and “User B” each hold a token.
Both reach one purple bookmark router carrying five badges: create, list, detail,
patch, delete. Behind it, a red “current user” identity card. Every arrow into the
orange bookmark service carries the user-id tag, teaching scope begins before repository
access.

At center, the service has three duties: “validate URL/title”, “material change policy”,
“normalize tags”. A blue repository query reaches SQLite: user-owned bookmarks, reusable
tags, and a `bookmark_tags` link card. Draw a thick blue boundary around User A records;
User B trying to read, patch, or delete A’s bookmark hits one purple “not found” card,
not an ownership-revealing response.

Below, show raw “ Python ” and “python” going through a funnel trim → lowercase → reject
empty → deduplicate. Canonical associations reach the link table. Next to bookmarks,
show locked `created_at` and refreshable `updated_at` only after material scalar or tag
membership change. A gray no-op PATCH returns without timestamp/event. At far right,
a magenta post-commit arrow emits a content-free invalidation hint only after committed
material mutation. EXCLUDE fetch-then-compare ownership, tag deletion as side effect,
raw SQL CRUD, credentials, long text, 3D, gradients, and tables.

CRITICAL IMPLEMENTATION VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector ownership state-machine schematic, 2K wide 16:9, pure-white background. Use a center-focused state machine, but render it as physical cards rather
than UML syntax. Operation tabs enter from left in one vertical column. The owner
predicate tag is repeated on every blue database arrow, intentionally reinforcing the
rule. Database outcomes are two clearly separated right-side ports: green owned result
and purple absence result. Above the service, the tag pipeline needs a visible rejected
blank chip and a duplicate chip merging with its canonical twin; below it, an orphan tag
card remains in the tags area when a link arrow disappears. The material fork uses a
green path that touches exactly commit, timestamp, event; the gray no-op path visibly
skips them. Keep created_at static and locked. Bottom edge probes are small round test
markers with one-to-three-word labels. Red crossed-out anti-patterns must be peripheral,
so correct state flow remains the dominant story.

VISUAL DETAIL: Generate a 2K wide 16:9 state/mutation diagram. Put the orange
Bookmark Service in a prominent center rounded rectangle. Place a red authenticated
identity badge and purple request gate left of it; arrange the five operations as small
purple input tabs. Right of the service, draw one blue database card with two outcome
ports: owned row (green) and no row (purple 404). Make the owner predicate a bold blue
tag visibly travelling on every repository arrow. Above center, place a left-to-right
orange tag funnel with five concrete small cards; below center, place a fork with a
green material-change path and a gray no-op path. The material path must visibly touch
commit, updated_at, and event cards; the no-op path must bypass all three. Put created_at
as a locked blue card shared by both. Use red crossed-out alternatives for insecure
ownership and automatic orphan deletion. Use grouped solid/dashed arrows and concise
edge probes along bottom; no detached explanations.

Flat 2D vector ownership-and-mutation state diagram on a pure white background. Wide
16:9 landscape centered on “Bookmark service”.

An authenticated red “user_id A” badge enters a purple request gate. Five operations
feed the service. Every repository arrow visibly carries `WHERE bookmark.user_id = A`.
The database returns owned row or no row; no row always becomes purple 404. Below it,
cross out “fetch any row then compare owner”. Above, show tag transformation raw list →
trim → lowercase → reject blank → preserve first canonical value → link rows. Keep
orphan tags visible after their last link disappears; annotate “deliberate retention”.

In the center, compare two PATCH paths: green “material delta” changes scalar/tag state,
commits once, advances `updated_at`, then emits event; gray “semantic no-op” does no
write, no timestamp bump, no event. `created_at` remains locked in both. Bottom edge
chips: duplicate tags, blank tag, cross-user detail, unchanged PATCH, delete then
missing. Exclude automatic orphan cleanup, public RBAC, generic TODOs, 3D, gradients,
and tables.
