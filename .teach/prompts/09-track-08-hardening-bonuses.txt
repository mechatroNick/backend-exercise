TRACK 08 — FINAL HARDENING: SEED DATA, DOCKER, RATE LIMITING, AND CURSOR PAGINATION

Purpose: show how optional assessment scope was deliberately added only after the
mandatory gate, without weakening core behavior. Read with `.tracks/08-final-handoff/SPEC.md`,
ADR-007, `app/seed.py`, `Dockerfile`, `scripts/docker-entrypoint.sh`,
`app/api/rate_limit.py`, and `app/bookmarks/pagination.py`.

ARCHITECTURE VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector delivery-hardening schematic, 2K wide 16:9, pure-white background. Keep all four lanes the same visual weight because each was independently
verified after the core gate. Seed lane uses a small terminal, amber safety diamond,
blue database cylinder, and repeat-arrow icon; no production data labels. Docker lane
uses a single indigo container outline containing app, one worker, data mount, and health
probe, with the migration card clearly before server execution. Rate lane should use a
red boundary shield, opaque fingerprint disc, orange bucket with level marks, and purple
429 envelope; the opaque identity never shows user/peer value. Cursor lane uses a sealed
purple token capsule, verification shield, blue sorted row deck, and matching slate list
envelope. Place narrowly scoped red constraint cards under lanes: safe target, one worker,
bounded local state, owner/filter binding. All four lanes converge only after their green
test probes, so combined regression is visibly last. Use arrow labels such as “validate”,
“migrate”, “refill”, “sign”, “verify”, “traverse”; no generic delivery imagery.

VISUAL DETAIL: Generate a 2K wide 16:9 four-lane delivery-hardening schematic. Place
a forest-green “mandatory core gate” card at far left and make it feed four equal
horizontal lanes, each independently bounded. Lane one uses orange safety cards and a
blue database cylinder: seed command, migration-head check, fixed fixture, idempotent
rerun. Lane two uses muted-indigo container shapes: Docker image, non-root app,
Alembic-before-server entrypoint, one Uvicorn worker, mounted data volume, health probe,
SIGTERM. Lane three uses brick-red request shield, opaque HMAC identity card, orange
token bucket, compact purple 429 envelope. Lane four uses deep-purple list route,
sealed cursor token, orange verification gate, blue keyset scan, slate same-body response.
Put a small red safety gate under each lane for its central failure constraint. Use thick
solid arrows for operator/runtime lifecycle, dashed arrows for metadata, green arrows
for verified completion. All lanes merge into a single green clean-source regression
receipt at right. Use short labels, concrete icons/shapes, no legend or vague “bonus”
cloud; no secrets, production clusters, 3D, gradients, or tables.

Flat 2D vector delivery-hardening schematic on pure white. Wide 16:9 landscape. At
left, green “mandatory core gate passed” feeds four separate, bounded lanes—never a
single unstructured bonus pile. Use slate for operator packaging, red for limits,
purple for cursor transport, blue for SQLite data, orange for deterministic utilities.

Top lane: orange “safe seed command” points to named disposable development SQLite
database after migration. Show fixed fictional users/bookmarks/tags, idempotent rerun,
and red safety gate “reject unsafe target; never create schema; never delete data”.
Second lane: an indigo Docker container shows non-root app user, one Uvicorn worker,
mounted `/data` SQLite volume, entrypoint “Alembic migration then exec server”, liveness
healthcheck, graceful SIGTERM. Keep it clearly a local assessment container, not a
production cluster.

Third lane: red local rate limiter sits before selected auth/bookmark operations. Show
opaque HMAC-derived bucket keys, bounded in-memory state, `Retry-After`, exact 429
envelope, and a label “one worker, reset on restart”. It must not rate-limit docs,
health, unknown routes, or bypass authentication precedence. Fourth lane: purple
cursor mode attaches an opaque signed `X-Next-Cursor` token to a stable owner/filter
bound list traversal while retaining the normal response body. Show malformed or
mismatched cursor reaching one safe error path.

At right, all lanes enter a green “combined regression + clean-source verification”
receipt. Bottom boundary strip: “no Redis”, “no multi-worker coordination”, “no
public weekly history”. EXCLUDE production-cloud imagery, raw token secrets, rate-limit
identities, database credentials, 3D, gradients, and tables.

CRITICAL IMPLEMENTATION VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector hardening decision schematic, 2K wide 16:9, pure-white background. Each quadrant has a compact header and uses the same internal grammar:
input on left, safety/verification gate center, durable or response result right, red
failure exits below. Seed command is a slate terminal with no literal environment values;
Docker image is an indigo box with migration arrow positioned before server. Rate bucket
should look bounded and in-memory, with clock/refill arrow and no distributed nodes.
Cursor token should have a lock/seal icon and its verification checks should be tiny red
shields around a central gate. Draw post-gate success arrows green only where testing
proves the condition; red exception paths should terminate at one safe short error shape.
The shared test-evidence strip uses four instruments—repeat run, lifecycle probe, bucket
isolation, stable traversal—not a text-heavy report. Maintain strong whitespace between
quadrants and avoid collision of arrow labels or hidden data values.

VISUAL DETAIL: Generate a 2K wide 16:9 four-quadrant decision diagram, separated by
generous white gutters but connected to one green test-evidence strip at bottom.
Top-left seed quadrant: orange command card → blue migration-head gate → fixture upsert
cards → commit, with red unsafe-target/wrong-head/conflict exits. Top-right Docker
quadrant: indigo image box → orange migration-only box → indigo server-exec box → rose
health probe → gray SIGTERM arrow → green cleanup. Bottom-left rate quadrant: purple
request plus red policy badge → opaque HMAC digest disc → orange bucket clock/permit
diamond → slate response or purple 429 envelope, with red “ignore XFF” and “401 first”
tags. Bottom-right cursor quadrant: purple query/filter/order card plus blue last-key
card → sealed token → red verification checks → blue keyset predicate → slate list
body; tamper/expiry/mismatch follow red arrow to one safe error. Use arrows explicitly,
short labels only, no code, token values, multi-node claims, 3D, gradients, or tables.

Flat 2D vector four-quadrant hardening decision diagram on pure white. Top-left “seed
safety”: command-scoped environment → migration-head check → fixture upsert → commit,
with red exits for production/unsafe database, wrong migration, conflicting identity.
Top-right “Docker lifecycle”: image build → migration-only step → one-server exec →
live health probe → SIGTERM → cleanup; no image pass before these phases.

Bottom-left “rate limit”: request identity becomes opaque HMAC digest, then a locked
token bucket calculates permit or 429. Show refill clock, bounded key capacity, no-store
429 response, and safe policy-only event logging. Add red labels “ignore X-Forwarded-For”
and “auth 401 before user bucket”. Bottom-right “cursor”: query’s owner/filter/order
state plus last key are signed into opaque token; next request verifies signature, owner,
filters, expiry, and page mode before applying keyset predicate. A red side path shows
tampered/expired/mismatched token becomes the canonical cursor error. All quadrants end
at tests for idempotency, container lifecycle, bucket isolation/capacity, and traversal
stability. Exclude implementation code, literal secrets, multi-node rate limiting,
3D, gradients, and tables.
