TRACK 02 — ERROR CONTRACT, REGISTRATION, LOGIN, AND JWT AUTHENTICATION

Purpose: show the secure reusable identity boundary. Read with
`.tracks/02-auth-errors/SPEC.md`, `app/auth/`, `app/core/errors.py`, and `app/api/errors.py`.

ARCHITECTURE VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector security architecture schematic, 2K wide 16:9, pure-white background. Keep registration and login visibly parallel until their necessary
differences. Registration uses an orange identity funnel followed by red password shield
and blue persistence cylinder; login uses a red verification shield that receives two
gray failure inputs and renders them indistinguishable after convergence. The public
success response should be a small slate identity card next to a sealed token envelope,
never a full profile or claim dump. Draw the protected-resource route as a lower lane
that reuses the same red bearer shield, making reuse clear. Place the unified error
translator as one rose rounded rectangle spanning all lanes, with five small incoming
error-type chips but only compact output envelopes. For safe logging, use a rose lockbox
with a redacted document icon; no flow may enter it from token/password payloads. Use
solid arrows for successful transitions, gray dashed arrows for rejected input, and red
arrows only when a request terminates. Typography is clean sans-serif, bold short labels,
small gray purpose captions, no explanatory paragraphs.

VISUAL DETAIL: Generate a 2K wide 16:9 security architecture diagram with three
horizontal lanes: registration at top, login in the middle, and protected-resource
access at bottom. Put the charcoal client on the left, one deep-purple auth router just
right of it, orange service steps in the center, a blue users-table cylinder to the
right, and rose/red response and logging boundaries along the far-right edge. Render
identity canonicalization as an orange funnel, password handling as a brick-red shield,
Argon2 as a red lock, and the JWT as a sealed red envelope; show only its three compact
claim labels. Make generic failures literally converge into one visually identical 401
card. Use thick solid arrows for valid paths, thin gray arrows for alternative invalid
inputs, and red termination arrows into the error translator. Each node needs a simple
shape, color, icon, short label, and five-word purpose annotation; keep credential
material abstract and never paint literal token/password characters.

Flat 2D vector security-focused technical schematic on a pure white background. Wide
16:9 landscape, educational medium-high density. Charcoal client, purple HTTP, orange
services, blue persistence, brick-red security, rose safe-error boundary.

On the left, one client sends “register” and “login” requests into a purple auth router.
Registration flows through orange “canonical identity” with “trim + lowercase”, red
“password normalization” with “NFC, never trim”, red “Argon2 hash”, then a blue users
table with unique email and username. Its success returns a compact public-user card
and a sealed red access-token envelope.

Login begins at the same router and goes through a red “generic credential check”.
Show “unknown email” and “wrong password” as different gray inputs converging into one
identical red “invalid credentials” result to teach non-enumeration. Successful login
verifies Argon2 then reaches an orange JWT issuer. Show only short token claims `sub`,
`iat`, `exp` and “access-only, 30 min default”.

On the right, a protected bookmark request carries the sealed envelope to a red bearer
dependency. It verifies signature, expiry, and current subject, then passes a purple
“authenticated user” context to later routes. Below all paths, one rose HTTP-error
translator maps typed errors to a compact `error: code, message, details` envelope.
Unexpected failures enter a redacted rose log boundary and return safe 500. EXCLUDE
token values, passwords, hashes, stack traces, refresh tokens, RBAC, tables, 3D,
gradients, and photorealism.

CRITICAL IMPLEMENTATION VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector security decision schematic, 2K wide 16:9, pure-white background. Arrange the three lanes with equal height and make the central shield the
focal object. Input pills should have clear arrowheads into normalizers rather than
implying validation. The rollback loop must be a circular orange arrow from database
collision back to typed decision, clearly distinct from the red unexpected-failure exit.
Use one identical purple 401 envelope for all invalid-token cases, so the absence of
enumeration is visually obvious. Place current-subject lookup as a small blue user card
after token verification and before repository access. Every check around the token
envelope is a tiny shield/check symbol with short label, spaced evenly; failures merge
downward to the same 401. The owner-scoped predicate tag should sit directly on the blue
repository arrow, while the insecure alternative remains pale and crossed out. Bottom
probes should be colored green but read as test inputs, not claims of a production SOC.

VISUAL DETAIL: Generate a 2K wide 16:9 security decision diagram with three broad
horizontal swimlanes. In the untrusted lane use pale-gray input pills; in the service
lane use orange process cards and a blue insert cylinder; in the HTTP lane use purple
response envelopes. Center a large brick-red authentication shield that connects all
lanes. Use a small red crossed-out card for prohibited password normalization and a
second crossed-out repository card for fetch-then-compare ownership. Make the known
unique-conflict branch a visible rollback loop returning to a purple 409 envelope, but
draw unrelated integrity failure through a rose redaction boundary to safe 500. Render
the token verification checklist as four small red check nodes around the sealed
envelope, all pointing to one 401 card on failure. Use solid arrows for success,
dashed arrows for validations, and red arrows for failure convergence. Put green
evidence probes on the bottom edge, no detached legend or paragraphs.

Flat 2D vector security decision diagram on pure white. Center a red shield
“authentication boundary”; use three horizontal lanes: “untrusted input”, “typed
service”, “HTTP response”.

Top lane: email becomes one canonical lowercase identity; password goes through NFC
normalization directly to Argon2. Put a crossed-out red card “trim/lowercase password”.
Middle lane: registration attempts a blue database insert; a known identity-uniqueness
collision rolls back then becomes typed conflict 409, while unrelated integrity error
goes to redacted unexpected failure. Teach that pre-checks are not race-safe authority.

Show login receiving “unknown identity” or “bad password” and deliberately emitting the
same generic typed auth failure. Add JWT verification checklist around an envelope:
format, HS256 signature, `sub`, expiry, current user. All failures converge to one 401
envelope. End with a repository arrow carrying verified user id in the SQL predicate,
with crossed-out alternative “fetch arbitrary row then compare owner”. Bottom probes:
“tampered”, “expired”, “deleted subject”, “no credential leak”. Exclude real secrets,
payload dumps, unredacted errors, long prose, tables, 3D, and gradients.
