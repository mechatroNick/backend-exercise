TRACK 06 — EVENT-DRIVEN INVALIDATION, CURRENT SNAPSHOTS, HEALTH, AND LOGS

Purpose: teach the local reliability extension: durable truth plus an in-memory wake-up
hint, with canonical live fallback. Read with `.tracks/06-event-driven-stats/SPEC.md`,
`app/bookmarks/events.py`, `app/bookmarks/stats/dirty.py`,
`app/bookmarks/stats/publisher.py`, `app/bookmarks/stats/refresher.py`,
`app/bookmarks/stats/snapshots.py`, `app/bookmarks/stats/service.py`,
`app/api/health.py`, and `app/core/logging.py`.

ARCHITECTURE VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector reliability schematic, 2K wide 16:9, pure-white background. Make the durable marker card visibly larger than the queue event to state
their different authority. The blue database cylinder should include a small dirty-window
drawer beside canonical bookmark data; arrows from mutation touch both before commit.
The queue is a shallow magenta tray, while the refresher is a tall magenta rounded card
with a small named-thread label and a rose lifespan bracket. Draw snapshot as a teal
sealed card with a small freshness clock; live SQL as an equally prominent teal query
card, so neither looks like an error fallback. The request path reaches a decision
diamond and splits into two similarly weighted response arrows. Health is a small rose
probe cluster under worker/database/publisher, emitting only a compact readiness result.
Logs use rose line cards leaving state transitions, with a red privacy filter before any
output. Every arrow carries a two-word action label such as “mark dirty”, “commit”,
“wake”, “compute”, “publish”, “serve”.

VISUAL DETAIL: Generate a 2K wide 16:9 hub-and-spoke reliability schematic. Put a
large royal-blue durable SQLite cylinder in the center-bottom as source of truth; put an
orange Bookmark Service left, a small magenta bounded queue above it, a large magenta
named Stats Refresher center-right, and two equally visible results right: teal immutable
snapshot and teal live raw-SQL fallback. Use a purple request route and brick-red
user-scope badge at far left. Render dirty state as a blue marker card with fields
“user”, “UTC week”, “generation”; put a green commit stamp between it and the
content-free queue event. Give overflow/loss a gray queue-drop icon with a blue durable
marker arrow continuing beneath it. Render worker ownership as a rose FastAPI lifespan
bracket around its thread card. Use thick blue arrows for durable transactions, magenta
arrows for wake-up hints, orange arrows for computation, teal arrows for current-stats
outputs, and rose dashed arrows for health/log transitions. Integrate readiness as a
small rose decision diamond, never a public diagnostics dashboard.

Flat 2D vector event-driven technical schematic on pure white, wide 16:9, medium-high
density. Use purple for HTTP, orange computation, blue durable SQLite, magenta workflow,
teal statistics, rose observability, red failure/safety.

Left: an authenticated material bookmark mutation enters orange Bookmark Service. In the
same blue SQLite transaction it updates bookmarks and increments a durable dirty marker
keyed by “user + UTC Monday window + generation”. Only after a green commit stamp does a
small magenta content-free event enter a bounded in-process queue. Put a gray queue
overflow icon beside it and an annotation “hint may be lost; durable marker remains”.

Center: a single named non-daemon “Stats Refresher” worker, owned by FastAPI lifespan,
drains hints, coalesces users, and reconciles durable markers. It obtains canonical
current aggregates through the teal raw-SQL reader, then uses a blue immutable snapshot
store. Make an epoch/generation guard visible between calculation and publish so stale
work cannot overwrite newer state. The worker acknowledges only the observed marker
generation; a newer mutation survives for retry.

Right: `GET stats` first asks “fresh valid snapshot?” Green yes returns snapshot with
same body semantics; red no falls back to canonical live raw SQL. Below, readiness reads
database, worker, publisher, dirty backlog, and projection state, then exposes only
“ready / not ready”. Liveness stays independent. A rose JSON-lines logger records safe
state transitions, not tokens, SQL, bodies, or identities. EXCLUDE broker clusters,
multiple web workers, public projection history, queue as source of truth, 3D,
gradients, and tables.

CRITICAL IMPLEMENTATION VIEW — IMAGE-GENERATION PROMPT

RENDERING BRIEF: Flat 2D vector concurrency timeline schematic, 2K wide 16:9, pure-white background. Space the G7 and G8 moments horizontally to make causality unmistakable.
Use a blue row card with a small generation badge at each change and a green commit stamp
under each request write. The dropped event is a cracked magenta capsule, but its gray
dashed path must not obscure the continuing blue durable path. Worker calculation is a
large orange gear outside the snapshot lock, followed by a small red/green comparison
diamond. Stale G7 has two red crossed-out arrows—one toward snapshot, one toward ack—so
both guarded effects are visible. Successful G8 has a sealed teal snapshot and a blue
acknowledged marker card. The twin stats responses at right use the same body icon and
only header ribbons differ. Health uses simple status lamps, not technical internals.
Keep test chips low and small; no actual user or bookmark data appears anywhere.

VISUAL DETAIL: Generate a 2K wide 16:9 three-lane concurrency timeline. Use pale
purple request transaction, pale-magenta queue hint, pale-orange refresher lanes. In
request lane, show one blue dirty-marker record changing G7 to G8; in queue lane, show
a magenta event capsule and a gray dropped capsule; in worker lane, show read G7,
off-lock calculation, compare/publish gate, guarded acknowledgement, and later G8 retry
cards. Make stale G7 publish/ack end at red crossed-out stop while G8 ends in green
snapshot plus completed marker. At far right show two identical slate stats-body
envelopes—snapshot and live SQL—with only tiny header badges differing. Along bottom,
use a rose readiness/liveness pair: degraded readiness amber/red, independent liveness
green. Use explicit solid arrows for causal order and dashed arrows for optional queue
hint; short labels only, no code, secrets, second worker, 3D, gradients, or tables.

Flat 2D vector generation-race timeline on pure white. Three horizontal lanes: “request
transaction”, “queue hint”, “refresher worker”.

In request lane, mutation writes durable marker generation 7 then commits; only then a
magenta event is offered to queue. Show event drop/overflow as acceptable because marker
7 remains blue in SQLite. In worker lane, worker reads generation 7 and computes a
candidate outside the snapshot lock. Before it publishes, show a second request raising
the durable marker to generation 8. Worker’s compare-and-publish check detects stale 7:
cross out snapshot publish and guarded acknowledgement. A later worker cycle observes 8,
recalculates, publishes immutable snapshot, and acknowledges generation 8.

At right, show two response branches with identical `stats` body: fresh snapshot versus
live SQL fallback. Bottom health lane shows worker failure → readiness degraded while
liveness remains green and live SQL still works. A rose log boundary emits bounded safe
event names only. Edge chips: queue loss, stale candidate, worker disabled, dirty
backlog, shutdown. Exclude exactly-once event claims, sensitive payloads, detailed
health disclosure, multi-worker topology, 3D, gradients, and tables.
